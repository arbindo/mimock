version: "3.3"
services:
  postgres-db:
    container_name: pg_container
    image: postgres:13.5-alpine
    restart: on-failure
    env_file:
      - local.env
    ports:
      - "5432:5432"

networks:
  mimock-network:
    driver: bridge
